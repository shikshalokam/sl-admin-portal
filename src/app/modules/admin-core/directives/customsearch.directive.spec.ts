import { CustomsearchDirective } from './customsearch.directive';

describe('CustomsearchDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomsearchDirective();
    expect(directive).toBeTruthy();
  });
});
